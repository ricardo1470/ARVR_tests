using UnityEngine;

public class XRbutton : MonoBehaviour
{
    public void printMessage()
    {
        Debug.Log($"<color=green><b> make income! </b></color>");
    }
    // Start is called before the first frame update
    void Start()
    {
    }

    // Update is called once per frame
    void Update()
    {
    }
}
